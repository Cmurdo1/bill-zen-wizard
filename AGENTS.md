# AGENTS.md

Guidance for AI coding agents working in this repository:

- Follow existing project conventions and keep changes minimal.
- Run `npm run build` (and `npm run lint`) before pushing changes.
- Do not commit secrets — `.env` and `supabase/.temp/` are gitignored.
